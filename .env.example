# Overleaf Configuration Example
# Copy this file to ~/.overleaf-zed/config.json and fill in your details

# Example config.json structure:
# {
#   "cookie": "overleaf_session2=YOUR_SESSION_COOKIE_HERE",
#   "email": "your.email@example.com",
#   "serverUrl": "https://www.overleaf.com"
# }

# How to get your cookie:
# 1. Login to Overleaf in your browser
# 2. Press F12 to open Developer Tools
# 3. Go to Console tab
# 4. Run: document.cookie.split(';').find(c => c.includes('overleaf_session2'))
# 5. Copy the output (format: overleaf_session2=s%3A...)

# For self-hosted Overleaf instances:
# Change serverUrl to your server address, e.g.:
# "serverUrl": "https://overleaf.mycompany.com"
